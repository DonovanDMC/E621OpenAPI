summary: Tag Type Versions Index
operationId: tag_type_versions#index
tags:
  - Tag Type Versions
parameters:
  - $ref: "../../components/parameters/query/limit.yaml"
  - $ref: "../../components/parameters/query/page.yaml"
  - $ref: "../../components/parameters/query/id.yaml"
  - name: search[tag]
    in: query
    schema:
      type: string
  - name: search[user_id]
    in: query
    schema:
      type: string
  - name: search[user_name]
    in: query
    schema:
      type: string
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "../../components/schemas/tagTypeVersion.yaml"
