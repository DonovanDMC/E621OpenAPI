summary: Get Tag Implication
operationId: tag_implications#show
tags:
  - Tag Implications
parameters:
  - $ref: "../../components/parameters/path/id.yaml"
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/models/TagImplication.yml"
  404:
    $ref: "../../components/responses/notFound.yaml"
