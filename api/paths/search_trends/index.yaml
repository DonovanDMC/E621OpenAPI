summary: Search Trends Index
operationId: search_trends#index
tags:
  - Search Trends
parameters:
  - in: query
    name: day
    schema:
      type: string
      format: date
  - $ref: "../../components/parameters/query/limit.yaml"
  - $ref: "../../components/parameters/query/page.yaml"
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "../../components/schemas/models/SearchTrend.yml"
