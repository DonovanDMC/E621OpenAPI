summary: User Name Change Requests Index
operationId: user_name_change_requests#index
tags:
  - User Name Change Requests
security:
  - basicAuth: []
description: You must be Moderator+.
parameters:
  - $ref: "../../components/parameters/query/limit.yaml"
  - $ref: "../../components/parameters/query/page.yaml"
  - $ref: "../../components/parameters/query/id.yaml"
  - $ref: "../../components/parameters/query/order.yaml"
  - in: query
    name: search[current_id]
    schema:
      type: integer
  - in: query
    name: search[current_name]
    schema:
      type: string
  - in: query
    name: search[original_name]
    schema:
      type: string
  - in: query
    name: search[desired_name]
    schema:
      type: string
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "../../components/schemas/models/UserNameChangeRequest.yml"
