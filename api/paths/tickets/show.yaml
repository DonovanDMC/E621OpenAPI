summary: Get Ticket
operationId: tickets#show
tags:
  - Tickets
security:
  - basicAuth: []
description: You must be Staff+ to see tickets you did not create.
parameters:
  - $ref: "../../components/parameters/path/id.yaml"
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/models/Ticket.yml"
  403:
    $ref: "../../components/responses/accessDenied.yaml"
  404:
    $ref: "../../components/responses/notFound.yaml"
