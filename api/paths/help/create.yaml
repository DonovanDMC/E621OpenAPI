summary: Create Help Page
operationId: createHelpPage
tags:
  - Help Pages
security:
  - basicAuth: []
description: You must be Admin+.
requestBody:
  content:
    application/json:
      schema:
        type: object
        required:
          - name
          - wiki_page
        properties:
          name:
            type: string
          wiki_page:
            type: string
          related:
            type: string
            description: Separate with a comma followed by a space.
          title:
            type: string
responses:
  201:
    description: Success
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/helpPage.yaml"
  403:
    $ref: "../../components/responses/accessDenied.yaml"
  422:
    $ref: "../../components/responses/expectedError.yaml"
