summary: Note Versions Index
operationId: note_versions#index
tags:
  - Note Versions
parameters:
  - $ref: "../../components/parameters/query/limit.yaml"
  - $ref: "../../components/parameters/query/page.yaml"
  - $ref: "../../components/parameters/query/id.yaml"
  - $ref: "../../components/parameters/query/ipAddr.yaml"
  - $ref: "../../components/parameters/query/order.yaml"
  - in: query
    name: search[updater_id]
    schema:
      type: integer
  - in: query
    name: search[updater_name]
    schema:
      type: string
  - in: query
    name: search[post_id]
    schema:
      type: integer
  - in: query
    name: search[note_id]
    schema:
      type: integer
  - in: query
    name: search[is_active]
    schema:
      type: boolean
  - in: query
    name: search[body_matches]
    schema:
      type: string
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "../../components/schemas/models/NoteVersion.yml"
