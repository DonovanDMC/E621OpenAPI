summary: Delete Avoid Posting
operationId: avoid_postings#delete
tags:
  - Avoid Postings
security:
  - basicAuth: []
description: Soft deletion. Must have the bd staff user flag.
parameters:
  - $ref: "../../components/parameters/path/idOrName.yaml"
responses:
  204:
    description: Success
  403:
    $ref: "../../components/responses/accessDenied.yaml"
  404:
    $ref: "../../components/responses/notFound.yaml"
