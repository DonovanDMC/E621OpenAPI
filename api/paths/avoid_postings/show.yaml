summary: Get Avoid Posting
operationId: avoid_postings#show
tags:
  - Avoid Postings
parameters:
  - $ref: "../../components/parameters/path/idOrName.yaml"
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/avoidPosting.yaml"
  404:
    $ref: "../../components/responses/notFound.yaml"
