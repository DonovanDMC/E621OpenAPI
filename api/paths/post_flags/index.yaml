summary: Post Flags Index
operationId: post_flags#index
tags:
  - Post Flags
parameters:
  - $ref: "../../components/parameters/query/limit.yaml"
  - $ref: "../../components/parameters/query/page.yaml"
  - $ref: "../../components/parameters/query/id.yaml"
  - $ref: "../../components/parameters/query/ipAddr.yaml"
  - $ref: "../../components/parameters/query/order.yaml"
  - in: query
    name: search[reason_matches]
    schema:
      type: string
  - in: query
    name: search[creator_id]
    schema:
      type: integer
  - in: query
    name: search[creator_name]
    schema:
      type: string
  - in: query
    name: search[post_id]
    schema:
      type: integer
  - in: query
    name: search[post_tags_match]
    description: Only usable by Member+
    schema:
      type: string
  - in: query
    name: search[type]
    schema:
      type: string
  - in: query
    name: search[is_resolved]
    schema:
      type: boolean
responses:
  200:
    description: Success
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "../../components/schemas/models/PostFlag.yml"
