type: object
required:
  - comment
  - dmail
  - blip
  - post_flag
  - posts
  - users
properties:
  comment:
    $ref: "./sum.yaml"
  blip:
    $ref: "./sum.yaml"
  dmail:
    $ref: "./sum.yaml"
  post_flag:
    $ref: "./sum.yaml"
  posts:
    $ref: "./sum.yaml"
  users:
    $ref: "./sum.yaml"
  artist_version:
    description: If `search[with_history]=true`
    $ref: "./sum.yaml"
  note_version:
    description: If `search[with_history]=true`
    $ref: "./sum.yaml"
  pool_version:
    description: If `search[with_history]=true`
    $ref: "./sum.yaml"
  post_version:
    description: If `search[with_history]=true`
    $ref: "./sum.yaml"
  wiki_page_version:
    description: If `search[with_history]=true`
    $ref: "./sum.yaml"
